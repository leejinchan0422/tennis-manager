# TODO

## PM 결정 대기 (개발은 기본값으로 계속 진행 중)
- [ ] **순위 기준 확정** — 지금은 승 1점 / 무 0.5점 → 득실차 → 획득 게임.
      바꿀 곳: `src/lib/ranking.js` 의 `comparePlayers` 한 곳뿐.
- [ ] **한 게임 목표 점수** — 지금 6게임 기준(0~6 입력) 기본값. `src/lib/session.js` `DEFAULT_CONFIG.maxGames`
- [ ] **점수체계 공식 채택 여부** — 과거 데이터로 3~5회 검증 후 결정 (§10-⑤). 지금은 화면에 '공식 아님' 명시 중
- [x] ~~사파리 데이터 삭제 대응~~ → PWA 구조로 처리 (설치하면 데이터가 잘 안 지워짐). 0.3.0 완료
- [ ] **초기 회원 명단 입력 방식** — 한 명씩 vs 엑셀/CSV 붙여넣기
- [ ] **실력등급 자리 확보 여부** — MVP 제외(§10-④). 나중에 넣을 자리만 비워둘지

## PM 결정 대기 (REQ-ADMIN-001 관련)
- [ ] **해시 없는 기본 주소를 무엇으로 할지** — 지금은 관리자 화면. 로그인·PIN이 없으므로(§9) 주소를 아는 사람은 접근 가능.
      선택지: (가) 지금처럼 관리자 (나) 회원 조회 화면으로 바꾸고 운영진은 `#/admin` 사용
- [ ] **게스트 관리** — 요구사항 A에 있으나 미구현. 회원과 별도로 관리할지, 임시 회원으로 처리할지

## 배포 관련 결정 필요 (PM)
- [ ] **어디에 올릴지** — 설치·오프라인은 https에서만 됩니다. 무료 정적 호스팅(예: Netlify / Vercel / GitHub Pages) 중 택1 필요
- [ ] **기기 간 데이터 공유** — 지금은 기기마다 따로 저장(백업 파일로 이동). 여러 기기에서 동시에 같은 데이터를 보려면 서버가 필요합니다(방법 B). MVP 검증 후 결정

## 다음 개발
- [ ] 새 버전 배포 시 '새로고침하세요' 안내 UI (`public/sw.js` 의 CACHE 버전 갱신 감지)
- [x] ~~누적 랭킹 (여러 주 합산)~~ → 0.4.0 완료
- [x] ~~지난 운영일 목록 화면~~ → 0.4.1 완료 (대시보드 하단)
- [ ] 대진 수정 시 규칙 위반(같은 라운드 중복 등) 즉시 경고 표시
- [ ] 파트너 중복 하한선 계산을 성별 고려해서 정확하게 (`scheduler.js` `minRepeats`)

## 코드 안의 TODO/FIXME
- `src/lib/ranking.js` — 순위 기준
- `src/lib/session.js` — maxGames
- `src/lib/scheduler.js` — minRepeats 정밀화
- `src/features/dashboard/DashboardPage.jsx` — 샘플 명단 seed(). 실제 명단 입력 후 삭제 가능
- `src/features/ranking/RankingPage.jsx` — 누적 랭킹
